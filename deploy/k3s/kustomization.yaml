apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: 4caster

resources:
  - 00-namespace.yaml
  - 05-rbac.yaml
  - 10-secret.yaml
  - 20-deployment.yaml
  - 30-service.yaml
  - 40-cronjob.yaml

images:
  - name: 4caster
    newName: hud0shnik/myapp
    newTag: latest
